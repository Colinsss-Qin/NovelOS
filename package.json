{
  "name": "novelos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "ensure:dist": "node scripts/ensure-dist.js",
    "dev": "npm run build && npm run ensure:dist && node server.js",
    "prestart": "npm run build && npm run ensure:dist",
    "start": "node server.js",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "setup": "npm install && npm run prisma:generate && npm run prisma:push && npm run build && npm run ensure:dist"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "@prisma/adapter-libsql": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "express": "^4",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "prisma": "^7.8.0",
    "typescript": "^6.0.3"
  }
}
